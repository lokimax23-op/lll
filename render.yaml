services:
  - type: web
    name: classproject
    runtime: python
    buildCommand: bash ./build.sh
    startCommand: gunicorn classproject.wsgi:application --bind 0.0.0.0:$PORT
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.4
